let a=10;
let b="10";

// if(a==b)
//     console.log("both are same");

if(a===b)
    console.log("both a and b are same");    // in '===' it will check both datatype and content; in '==' it will only check content.